import GeocodeRequest, {
  GeocodeResultContext,
} from './GeocodeRequest'
import ReverseGeocodeRequest, {
  PlaceResultContext,
} from './ReverseGeocodeRequest'
import SearchManager, {
  SearchManagerContext,
} from './SearchManager'

const Search = {
  GeocodeRequest,
  GeocodeResultContext,
  PlaceResultContext,
  ReverseGeocodeRequest,
  SearchManager,
  SearchManagerContext,
}

export default Search
