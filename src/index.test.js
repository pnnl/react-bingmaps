import Microsoft from '.'

describe('Microsoft', () => {
  it('is truthy', () => {
    expect(Microsoft).toBeTruthy()
  })
})

describe('Microsoft.Maps', () => {
  it('is truthy', () => {
    expect(Microsoft.Maps).toBeTruthy()
  })
})
